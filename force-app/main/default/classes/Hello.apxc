public with sharing class Hello {
 

    public static void testme()
    {
        Boolean isProcessed = false;
        if(isProcessed == True) {
            System.debug('debugggg');
        } else {
            System.debug('debug output');
        }
    }
}